# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/

# Virtual environment
.venv/

# Environment / secrets
.env

# macOS
.DS_Store

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Runtime data (now lives in .baymax/ at project root)
*.db
*.db-shm
*.db-wal
*.db-journal

# Legacy doc/ — docs now live at project root baymax-docs/
doc/

# User config — now lives in .baymax/config/ at project root.
# Defaults ship as *.default.yaml (tracked).
config/project.yaml
config/nodes.yaml
config/mcp.yaml

# Legacy remnants
mcp_servers/
uv.lock

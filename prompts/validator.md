# DEPRECATED — Validator merged into Planner

The standalone Validator role has been removed. The Planner now handles validation
directly, since it already holds the full project context (data schema, algorithm
design, success criteria, edge cases).

See `prompts/planner.md` — the "Validation" section — for the current QA behavior.
